name: Auto GPU Version Updater

on:
  schedule:
    - cron: '0 0 * * *' # 毎日1回実行
  workflow_dispatch: # 手動実行用

jobs:
  update-gpu:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write # 👈 変数を書き換えるために必須の権限
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Run Updater
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # 自動発行されるトークン
          # GitHubのVariablesから現在の最新バージョンを読み込む
          CURRENT_BASE_VERSION: ${{ vars.LATEST_GPU_VERSION }}
          # 通知用（必要に応じて）
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: python gpu_updater.py
