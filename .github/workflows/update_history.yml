name: Update NVIDIA Driver History
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Prepare Git
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git checkout main
          git pull origin main

      - name: Run update script
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python get_driver.py
          echo "--- DEBUG: Check output ---"
          # ファイルがある時だけ中身を表示する（エラー防止）
          if [ -f driver_history.txt ]; then
            ls -l driver_history.txt
            cat driver_history.txt
          else
            echo "Warning: driver_history.txt was not created."
          fi

      - name: Commit and Push
        run: |
          # ファイルが存在し、中身がある時だけプッシュを試みる
          if [ -s driver_history.txt ]; then
            git add driver_history.txt
            git commit --allow-empty -m "Update driver: $(date +'%Y-%m-%d %H:%M')"
            git push origin HEAD:main
          else
            echo "Nothing to push."
          fi
